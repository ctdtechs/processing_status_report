# Bootstrap creds for the config DB. .env holds the CONFIG_DB_* values and
# MUST NOT be committed. .env.example (the template) is committed.
.env

# Storage-alert reminder state (runtime)
.psr_storage_state.json

# Logs
status_report.log

__pycache__/
*.pyc
